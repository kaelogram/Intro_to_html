https://developer.mozilla.org/en-US/docs/Web/HTML/Guides/Cheatsheet

For each of the HTML tags in the link above, write a short description on how they can be used.

State the difference between inline element and block elements in HTML.

a /a – Used to create a hyperlink in HTML

img / – Used to insert an image into a webpage

span /span – Used to group or style inline elements

em /em – Used to emphasize text

i /i – Used to display text in italics

b /b – Used to make text bold

strong /strong – Used to indicate that text is important

mark /mark – Used to highlight text and make it noticeable

s /s – Used to show text that is no longer relevant or has been deleted by striking through it

sub /sub – Used to display subscript text (e.g., H₂O).

small /small – Used for fine print or to make text appear smaller.

address /address – Used to display contact information or a physical address.

cite /cite – Used to reference the title of a work

sup /sup – Used to display superscript text

q /q – Used to define a short, inline quotation.

br / – Used to insert a line break.

wbr / – Used to suggest a possible line break within a long word

time /time – Used to represent a specific time or date

code /code – Used to display text in a code format.

audio /audio – Used to insert an audio file into a webpage.

video /video – Used to insert a video file into a webpage.

p /p – Used to create a paragraph of text.

blockquote /blockquote – Used to indicate an extended quotation.

details /details – 

ul /ul – Used to create an unordered list

ol /ol – Used to create an ordered list

dl /dl – Used to define a description list containing terms and their definitions

hr / – Used to insert a horizontal line or divider

h1–h6 – Used to define headings, from largest (h1) to smallest (h6)


Block Elements start on a new line and take up the full width available stretching from left to right while inline elements do not start on a new line and only take up as much width as the content inside them

Block elements are used for layout and sections while inline elements are used for styling within text



More Tags that are not in the link above 

div /div - it is a container that hold stuffs. 